# Django Settings
DEBUG=False
SECRET_KEY=secure_production_key_change_this
ALLOWED_HOSTS=pawnshop-z817.onrender.com,localhost,127.0.0.1

# Database Configuration - SQLite for all environments
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3

# Email Settings
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-email-password
DEFAULT_FROM_EMAIL=Pawnshop Management <no-reply@example.com>

# Face Recognition Settings
FACE_RECOGNITION_MODEL=hog
FACE_RECOGNITION_TOLERANCE=0.6

# Security Settings
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True